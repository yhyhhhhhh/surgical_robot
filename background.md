我正在做手术机器人自主操作的仿真实验，我的主要场景是控制ur3机械臂在一个狭窄管道内完成血块的夹取，其中管道非常狭小，直径大概只有5cm，血块的大小也非常小，大小在厘米级。
我主要采用强化学习的方式实现机器人自主操作，我的动作定义如下
 5维连续动作（每个维度通常在 [-1, 1]）：
    actions[:, 0]：Δs，沿管道轴向前进/后退
    actions[:, 1]：Δr，朝管中心/管壁径向移动
    actions[:, 2]：Δθ，在管道截面内绕中心转动
    actions[:, 3]：Δyaw，末端绕自身轴旋转
    actions[:, 4]：夹爪开合控制（连续值），再经过速度限制更新 gripper_cmd
    
我的观测定义如下：
1. 机器人自身状态（proprioception）
- `s_e, r_e`：末端在管道坐标里的轴向深度和径向距离  
- `cos_th_e, sin_th_e`：末端角度 `th_e` 的三角编码（避免角度跳变）  
- `joint_pos, joint_vel`：关节相对默认位姿/速度  
- `grip_norm`：夹爪开合归一化值

2. 相对目标状态（goal-relative）
- `ds, dr`：物体相对末端的轴向/径向差  
- `cos_dth, sin_dth`：截面角度差 `dth` 的三角编码  
- `cos_dyaw, sin_dyaw`：末端与物体 yaw 差的三角编码（你新增的关键项）

3. 任务进度指示（dense hints）
- `object_lift`：物体相对初始高度变化（是否被抬起）  
- `g_lat_obs`：横向对齐进度（`e_lat` 越小越接近 1）  
- `g_close_obs`：可抓取进度（轴向接近且横向已对齐时更大）  
- `is_captured_obs`：几何规则判定是否夹住（0/1）

4. 环境约束感知
- `margin_to_wall`：离管壁余量（越小越危险）

目前我准备在强化学习的基础上进行世界模型的研究