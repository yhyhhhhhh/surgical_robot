# 在线训练世界模型
首先是通过dreamer训练了一个世界模型
接下来可能需要去探索一下怎么去看这个世界模型的不确定性比较高的区域
如何用离线的专家数据训练好一个世界模型

---
# 离线训练世界模型
首先是采集数据，考虑到目前的动作驱动方式1为增量式，如果直接用随机采集数据会导致采集的范围非常受限，并不适合用于覆盖状态空间，因此可能需要写一个随机生成状态的代码，来通过随机状态+随机动作来覆盖状态空间，训练一个基本的世界模型
然后是补充采集的专家数据，这两个构成一个数据集训练

---
# 不确定性分析
下一步就是在已有世界模型的基础上如何分析模型估计的不确定性

目前采用的是jrd的方式计算散度，遇到的一个问题就是如果策略全部用最大的不确定性的动作执行

